import 'package:flutter/widgets.dart';

class Paddings {
  static const textFieldPadding = EdgeInsets.all(8);
  static const iconButtonPadding = EdgeInsets.only(
    right: 10,
    top: 8,
    bottom: 8,
  );
  static const iconButtonContentPadding = EdgeInsets.all(17);
  static const phoneMesaggeCardPadding = EdgeInsets.fromLTRB(10, 10, 14, 10);
  static const computerMesaggeCardPadding = EdgeInsets.fromLTRB(14, 10, 10, 10);
}
