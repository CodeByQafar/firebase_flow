import 'package:flutter/widgets.dart';

class Paddings{
  static const textFieldPadding = EdgeInsets.all( 8);
static const iconButtonPadding = EdgeInsets.only(right: 10, top: 8, bottom: 8);
static const iconButtonContentPadding = EdgeInsets.all(17);
}